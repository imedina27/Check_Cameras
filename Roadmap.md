# Roadmap — Check Cameras V2

Basado en la revisión de `Check_Cam_V1`. Enfoque: **refactor incremental** — mismo propósito y arquitectura general, corrigiendo errores, optimizando el código y actualizando módulos clave. Sin rediseño mayor (sin dashboard/DB/alertas por ahora).

Orden: primero corregir lo que está roto o es frágil (Fase 1), luego optimizar el proceso secuencial tal como está (Fase 2), luego modernizar módulos (Fase 3), y al final paralelizar (Fase 4) — a propósito hasta el final, para paralelizar sobre una base ya corregida y optimizada.

## Notas de contexto importantes (no olvidar)

- **El programa es puramente de línea de comandos a propósito.** Corre en dos entornos con visibilidad de red distinta hacia las plantas — no está pensado para convertirse en un servicio/daemon sin repensar esto:
  - **Windows 11 (máquina del usuario):** tiene VPN Zerotier que ve las plantas. Usa `conf/plants/plants.yaml`.
  - **Ubuntu Server "QLYMSPROD01":** ve a los demás servidores directamente por red (sin VPN). Usa `conf/plants/plants_abinbev.yaml`. Aquí hay plantas en la misma red que se pueden agrupar por el campo `plant` del YAML.
- **`plants.yaml` y `plants_abinbev.yaml` NO son duplicados** — son configuraciones paralelas, una por entorno de ejecución. No proponer "unificarlos" sin más; el problema real es que `InfoPlant()` tiene hardcodeado el nombre `plants.yaml`, por lo que cambiar de entorno hoy requiere renombrar archivos a mano (ver Fase 1).

---

## Fase 0 — Preparación del proyecto

- [x] Copiar todos los archivos de `Check_Cam_V1` a la raíz del proyecto (`Check_Cameras`), dejando `Check_Cam_V1` como carpeta de referencia.
- [x] Instalar dependencias en el entorno virtual (`pipenv install`).
- [x] Actualizar `Pipfile` con las dependencias del proyecto y eliminar `requirements.txt` por redundante.
- [x] Hacer commit del proyecto.

---

## Fase 1 — Corrección de errores de programación

- [x] **`main.py` ignora los argumentos reales de CLI.** `sys.argv` se sobreescribe de forma fija a `--plant all` ([main.py:52-54](main.py#L52-L54)), dejando el `argparse` real sin efecto. Quitar el override y dejar que el script use los argumentos reales.
- [x] **Selección de `plants.yaml` vs `plants_abinbev.yaml` está hardcodeada.** Resuelto con una variable `PLANTS_FILE` en `.env` (config.py + `InfoPlant()` en file_processor.py), con default `plants.yaml`. Cada entorno fija su propio valor: `.env` local (Windows) → `plants.yaml`, `AbInBev.env` (plantilla del Ubuntu Server) → `plants_abinbev.yaml`. Al iniciar, se imprime qué archivo se cargó para detectar de inmediato una mala configuración.
- [x] **Gestión de túneles SSH usaba herramientas solo de Unix** (`ss -tulpn`, `os.kill`, `signal.SIGTERM/SIGKILL`, `pkill`). Reemplazado por `psutil` (nueva dependencia) en `check_tunnel_status`, `find_tunnel_pid`, `close_tunnel` y `alternative_close` — una sola implementación que funciona igual en Windows y Linux, sin parsear salida de comandos de shell. También se agregó `get_os()` como helper reutilizable de detección de SO (usado ya en `ping()`). Probado extremo a extremo en Windows: detecta puerto en uso, encuentra el PID real y cierra el proceso correctamente.
- [x] **`locale.setlocale(locale.LC_TIME, 'es_MX.UTF-8')` podía tronar la ejecución completa** si el locale no estaba instalado en la máquina. Reemplazado por un diccionario `MESES_ES` en `file_processor.py`, sin depender del locale del sistema operativo.
- [x] **Sin manejo de claves faltantes en YAML.** `CheckServer()` accedía directo a `dat_server['addresses']['local']`, `dat_server['ia_ports']`, etc. y un YAML de planta incompleto tumbaba toda la corrida. Se agregó validación explícita al inicio de `CheckServer()` (loguea qué campos faltan y salta ese servidor) y un envoltorio `_CheckServerSafe()` en `CheckAll`/`CheckPlant` que aísla cualquier error inesperado por servidor, para que el resto de la corrida continúe.
- [x] **Hallazgo en producción: `open(pid_file, 'w')` tronaba si la carpeta `tmp/` no existía.** Detectado al probar en Ubuntu: todos los servidores `type: tunnels` (casi todos, excepto QLYMSPROD01) fallaban con `FileNotFoundError` al guardar el PID del túnel SSH, abortando toda la revisión de ese servidor antes de llegar a las cámaras (por eso solo tenían `.log`, sin ninguna imagen). `open(..., 'w')` no crea carpetas intermedias. Se agregó `_write_pid_file()` en `system_processor.py` (crea la carpeta con `os.makedirs(..., exist_ok=True)` antes de escribir) y se usó en los 4 lugares donde se guardaba el PID. Probado reproduciendo el escenario exacto (carpeta `tmp` inexistente).
- [x] **Hallazgo en producción: `cam_activate` se trató como obligatorio por error.** Al probar en el servidor Ubuntu real, los 11 servidores de `plants_abinbev.yaml` (que nunca han usado ese campo) se marcaban como "Configuración incompleta" — bug preexistente desde V1 (ahí tronaba el programa completo; nuestra validación de Fase 1 lo hizo más visible pero seguía siendo incorrecto). Se quitó `cam_activate` de `REQUIRED_SERVER_KEYS` y el código ahora usa `dat_server.get('cam_activate', False)`. Se aclaró además la semántica (confirmada con el usuario): `cam_activate: true` filtra cámaras marcadas inactivas en el `cameras.yaml` del servicio IA; `false` revisa todas sin filtrar. Se dejó `cam_activate: false` explícito en los 4 servidores de `plants.yaml` y los 11 de `plants_abinbev.yaml`, para revisar todas las cámaras sin importar su estado individual. Probado: los 11 servidores de AB InBev ya no reportan campos faltantes.
- [x] **Credenciales de cámara expuestas en un `print` de error.** Si `InfoCam_url()` fallaba al parsear una URL, imprimía la URL completa (con usuario/contraseña en texto plano) a consola. Se agregó `mask_credentials()` en `file_processor.py` para enmascarar usuario/contraseña antes de imprimir, usado en ese `except`. El resto del flujo (peticiones HTTP a las cámaras) ya usaba `HTTPDigestAuth`/`HTTPBasicAuth`, que no exponen credenciales en la URL.
- [x] **Excepciones genéricas que ocultaban la causa real.** Se revisaron todos los `except Exception`/`except:` del proyecto: la mayoría en `system_processor.py` ya imprimían el error real y estaban bien como red de seguridad. Se corrigieron los dos casos silenciosos/engañosos: `InfoPlant()` (ya no descarta el error, lo imprime) y `read_yaml()` (el catch-all ya no reporta un error inesperado como "HTTP error [413]" — se agregó el código 415 "Unexpected error" para distinguirlo). También se corrigió `ping()`, que descartaba el error sin dejar rastro.
- [x] **Variables de `.env` que no coinciden con `config.py`.** `config.py` ahora lee `MAX_AI_RETRIES`/`AI_IMAGE_TIMEOUT` (antes buscaba `MAX_IMAGE_RETRIES`/`IMAGE_TIMEOUT`, que no existían en el `.env`). También se corrigió `PX_PID_FILE`/`IA_PID_FILE`, que ambas leían por error la misma clave inexistente `PID_FILE`.

## Fase 2 — Optimización de código

> La paralelización (el mayor cuello de botella) se pospuso a propósito hasta el final — ver **Fase 4**. Primero se mejora el proceso tal como está hoy (secuencial).

- [x] **Reutilizar sesiones HTTP.** Cada `Camera` ahora crea un `requests.Session()` compartido entre `Cam_Up`, `Cam_AI_Image`, `Cam_Image` y `Cam_Config` (y sus funciones de marca en `conf/cameras/*.py`, que ahora reciben `session` como parámetro en vez de usar `requests` directo). Probado extremo a extremo con un servidor HTTP local: las 6 peticiones de `DahuaCamConf` reutilizaron la misma conexión TCP en vez de abrir una nueva por cada una.
- [x] **Revisar acumulación de timeouts.** Decisión: no forzar un límite que corte reintentos — el objetivo de la herramienta es intentar todo lo posible por conseguir las 3 evidencias de cada cámara, no ser rápida. En vez de un techo que salte cámaras, se dará **visibilidad** del tiempo con un cronómetro por cámara — ver **Fase 4** (se hace justo antes de paralelizar, para poder comparar el tiempo total antes/después).
- [x] **Evitar trabajo repetido en `dirs_path()`/`log_processor()`.** Cada línea de log repetía `os.makedirs()` y `os.path.isfile()` (llamadas al sistema de archivos) para la misma carpeta/archivo, decenas de veces por servidor. Se agregó una caché en memoria (`_dirs_ensured` para directorios; el caché de loggers de la migración a `logging` en Fase 3 cubre el archivo) para que cada carpeta/archivo se verifique una sola vez por corrida. Probado con 50 llamadas seguidas al mismo servidor: `os.makedirs`/`os.path.isfile` se ejecutaron 1 sola vez cada uno (antes: 50), sin duplicar encabezados ni cambiar el contenido del log.

## Fase 3 — Actualización de módulos

- [x] **Reemplazar el despacho de marca por if/elif** en `cameras.py` (`Cam_Image`, `Cam_Config`) por un registro (`CAMERA_HANDLERS`, dict marca → `(func_imagen, func_config)`). Se uniformaron las firmas de las 8 funciones de marca (todas aceptan `channel=1`, aunque no todas lo usen aún) para que el registro no necesite casos especiales. Se dejó un comentario junto a `CAMERA_HANDLERS` recordando que `resolve_brand()` (`file_processor.py`) también debe actualizarse al agregar una marca — es un mapeo aparte (URL → marca) que no vive en el registro. Probado con las 4 marcas contra un servidor local + caso de marca desconocida (620/720).
- [x] **Unificar gestión de dependencias.** Resuelto en Fase 0: se eliminó `requirements.txt` y `Pipfile` quedó como única fuente de verdad.
- [x] **Logging estructurado (núcleo).** `log_processor()` en `file_processor.py` ahora usa el módulo `logging`: un logger por archivo (`FileHandler` + `StreamHandler` coloreado por nivel real, no por texto). Se quitó el prefijo redundante `[SUCCESS]/[WARNING]/[ERROR]` de `config.STATUS_MESSAGES` en pantalla/archivo (ya lo indica el nivel). Se eliminó el parámetro `add_timestamp` (ya no aplica: cada línea lleva timestamp automático) y se actualizaron las ~28 llamadas en `check.py`. Ajuste tras verlo corriendo en producción: las líneas estructurales (separadores, "Camera: X IP: Y") se muestran como `INFO` en texto pero **sin color** (nivel interno `STRUCTURE_LEVEL`), dejando el verde reservado a éxitos reales y el rojo a errores reales — así se distingue de un vistazo el cambio de servidor/cámara. Probado extremo a extremo con un servidor simulado (éxito y error), 20 llamadas repetidas al mismo servidor (sin duplicar handlers ni líneas), y verificación directa del formateador de color (estructural sin color, éxito verde, error rojo).
- [x] **Logging estructurado (barrido de `print()` sueltos).** Decisión: se pospuso a **Fase 4**, antes de la sección de paralelización (ver ahí el detalle y el orden).
- [x] **Resumen final de ejecución.** `CheckServer()` ahora regresa un resultado estructurado (servidor: problema o no + lista de cámaras con sus fallas), que `CheckAll`/`CheckPlant` acumulan y pasan a `write_summary()` (`file_processor.py`). Genera `resumen_dd-mm-aaaa.log` en la carpeta del día, con totales (servidores OK/con problemas, cámaras completas/con falla) y el detalle por planta de qué cámara falló y en qué paso. Solo aplica a `CheckAll` (siempre) y a `CheckPlant` cuando encuentra más de un servidor. Probado con 2 servidores (uno con configuración incompleta, otro con cámaras fallidas), verificado que no se genera con un solo servidor, y validado contra un resumen real de producción (88 cámaras). Ajuste tras verlo en producción: el detalle por planta ahora alinea en columnas (nombre de cámara e IP) según el más largo de cada planta, para que sea fácil de escanear visualmente.
- [x] **Pruebas automatizadas básicas.** Se agregó `pytest` (dev-dependency en `Pipfile`) y una carpeta `tests/` con 32 pruebas para funciones puras (sin red): `resolve_brand`, `InfoCam_url`, `mask_credentials` (incluyendo el caso de contraseña con `@`), `dic_to_json`/`txt_to_json` de las 4 marcas (incluyendo el caso límite de Axis/Vivotek perdiendo una línea con `=` en el valor, que Dahua sí conserva), `status_text`/`is_success_code`, y la validación de campos faltantes de `CheckServer()`. Correr con `pipenv run pytest`. Las 32 pruebas pasan.
- [x] **Hallazgo (Fase 1 revisited): `HikvCamConf` no atrapaba errores de XML malformado.** Solo capturaba `requests.exceptions.RequestException`; si la cámara respondía algo que `xmltodict` no podía parsear, la excepción (`xml.parsers.expat.ExpatError`) tumbaba toda la corrida. Se agregó el catch específico en `hikvision.py`, que ahora imprime el error real y devuelve 790 sin tronar. Probado con una respuesta XML malformada.

---

## Fase 4 — Paralelización

Se hace al final, una vez que el proceso secuencial ya esté optimizado y estable (Fases 1-3). Orden interno de esta fase: primero el barrido de logging, luego el cronómetro, al final la paralelización — cada uno se verifica en secuencial antes de mezclarlo con concurrencia.

### Barrido de `print()` sueltos hacia el log correcto (antes de paralelizar)

Se hace **antes** de paralelizar (no después): así cualquier cambio de firma/lógica se verifica en un entorno secuencial y determinista, sin mezclarlo con bugs de concurrencia al mismo tiempo.

- [x] **Enviar los `print()` huérfanos al log de la planta/servidor correcto**, no solo a consola. Implementado para las funciones que aplicaban:
  - **Túneles (`system_processor.py`):** `create_tunnel()`/`close_tunnel()` ahora reciben `plant` (y `close_tunnel()` también `serv_name`, que antes no recibía en absoluto), e internamente usan `log_processor()` en vez de `print()` — se importó `log_processor` desde `file_processor.py` (sin ciclo de imports, verificado). Las funciones auxiliares más internas (`find_tunnel_pid`, `check_tunnel_status`, `alternative_close`) se dejaron con `print()` por ahora, fuera de alcance de este punto.
  - **`ping()`, `read_yaml()`, `InfoCam_url()`:** se les agregó `plant`/`server` como parámetros **opcionales** (default `None`) — `log_processor()` ya maneja `None` sin tronar (cae a una carpeta/archivo `ERROR` genérico), así que siguen funcionando igual si se llaman sin contexto (ej. en las pruebas de pytest).
  - Se actualizaron todos los call sites en `check.py` para pasar `plant`/`serv_name`.
  - Probado extremo a extremo: falla de túnel simulada (sin red real) queda registrada en el log de la planta/servidor correcto; error de `read_yaml()` igual. Las 32 pruebas de pytest siguen pasando sin cambios (compatibilidad hacia atrás confirmada).
- [x] **Hallazgo en producción: `Cam_Image()`/`Cam_Config()` de las 4 marcas descartaban el detalle real del error.** Al probar en Ubuntu, varias cámaras mostraban "Configuración: Unknow error [790]" sin ninguna pista de la causa — `axis.py`/`dahua.py`/`vivotek.py` ni siquiera imprimían la excepción (`except requests.exceptions.RequestException as e: return 790`, la `e` nunca se usaba); solo Hikvision tenía un `print()` para el caso de XML inválido, y ni ese quedaba en el log. Se agregó `plant`/`server` opcionales a las 8 funciones de marca (`CamImage`/`CamConf` × 4), y ahora todas registran el detalle real (vía `log_processor()`) antes de regresar el código de error — incluyendo, para Hikvision/Dahua, cuál de las peticiones secuenciales de configuración falló. Probado extremo a extremo simulando un 401: el log ahora muestra "401 Client Error: Unauthorized..." en vez de solo "790".

### Cronómetro y visibilidad de tiempos por cámara (justo antes de paralelizar)

Se implementa aquí, inmediatamente antes de paralelizar, para poder comparar con datos reales el tiempo total antes/después del paralelismo.

- [x] **Cronómetro por cámara + log coloreado según umbral.** Un cronómetro (`time.perf_counter()`) arranca antes de `Cam_Up()` y se detiene justo después de `Cam_Config()` (un solo tiempo total, no desglosado por paso), justo antes del separador `====` de esa cámara. Se agregó `log_camera_time()` en `file_processor.py`: verde (`INFO`) si el tiempo quedó dentro de `config.CAM_TIME_THRESHOLD`, **amarillo** (`WARNING`) si lo excedió — el rojo queda reservado para errores reales, no para "solo lento". No se salta ni se cancela nada, es solo visibilidad. Umbral calculado con 3 logs reales de producción (QLYMSPROD01/02 locales en Zacatecas, QLYMSPROD05 por túnel en Tocancipa): se encontró que cámaras sanas pueden tardar 12-14s por diseño (Hikvision/Dahua hacen varias peticiones para armar su configuración, vs. 1 sola en Axis/Vivotek) independientemente de si es local o por túnel — se definió `CAM_TIME_THRESHOLD=15` (segundos) en `.env`/`AbInBev.env` como punto de partida, ajustable. Probado extremo a extremo: color correcto según umbral, e integrado en el ciclo real de `CheckServer()`.

### Paralelización

- [x] **Definir y crear los procesos de paralelización.** El procesamiento de cada cámara (`Cam_Up` + `Cam_AI_Image` + `Cam_Image` + `Cam_Config` + cronómetro) se extrajo a `_process_camera()`/`_process_camera_safe()` (esta última aísla errores, mismo patrón que `_CheckServerSafe()`) en `check.py`. El `for` secuencial de cámaras se reemplazó por `concurrent.futures.ThreadPoolExecutor(max_workers=config.MAX_CAMERA_WORKERS)` (I/O-bound, no requiere `asyncio`). Nueva variable `MAX_CAMERA_WORKERS=5` en `.env`/`AbInBev.env`/`config.py`, ajustable después con datos reales. Servidores entre sí se mantienen secuenciales — ver nota abajo.
  - **Cada línea de log ahora lleva el alias de la cámara al frente** (vía nuevo parámetro `prefix` en `log_processor()`), porque varias cámaras corren a la vez y sus líneas se intercalan en el log del servidor — sin el alias sería imposible saber de qué cámara es cada línea.
  - **Bug de concurrencia corregido antes de activar el paralelismo:** el caché de loggers de Fase 3 (`_get_server_logger()`) no era seguro para hilos — dos cámaras del mismo servidor logueando por primera vez casi al mismo tiempo podían duplicar los manejadores (líneas repetidas). Se agregó un `threading.Lock()` con patrón double-checked-locking. También se protegió con el mismo candado la escritura directa de líneas en blanco (bypaseaba el lock interno del `Handler`).
  - Probado extremo a extremo con 10 cámaras simuladas y 5 workers: **10.36s en paralelo vs. ~50s estimados en secuencial** (~5x, coincide con el número de workers), **0 líneas duplicadas** en el log, y alias correctamente distinguible en cada línea intercalada.
  - **Ajuste tras verlo en producción real:** con el log intercalado, "Camera: X IP: Y" y el separador `====` por cámara ya no aportaban (el separador no separa nada si está todo mezclado). Se quitó la palabra "Camera:" (queda `{alias} IP: {ip}`), se eliminó el separador por cámara, y `log_camera_time()` cambió de `"Tiempo de proceso: {alias}: {tiempo}s"` a `"{alias}: Tiempo de proceso {tiempo}s"` — todas las líneas de una cámara ahora siguen el mismo patrón `[hora] [nivel] {alias}: {info} [código]`.
- [x] **No paralelizar servidores entre sí (por ahora).** Decisión aplicada: el `for` de servidores en `CheckAll`/`CheckPlant` sigue secuencial — solo se paralelizan las cámaras dentro de un mismo servidor. Motivo: cuando `type: tunnels`, el túnel usa un puerto local fijo y compartido (`IA_LOC_PORT`/`PX_LOC_PORT`); dos servidores con túnel corriendo al mismo tiempo se "pisarían" el puerto. Paralelizar servidores requeriría antes resolver la asignación de puertos locales únicos por túnel concurrente — queda fuera de alcance de V2.
- [x] **Logging seguro para concurrencia.** Resuelto de forma anticipada en Fase 3 para la escritura de cada línea (los `Handler` ya traen su candado interno), y se completó ahora con el candado en `_get_server_logger()` para la creación del logger (ver arriba) — ese caso específico no estaba cubierto por el candado interno del `Handler`.

---

## Mejoras / Actualizaciones del Programa

Puntos identificados sobre la marcha, sin una fase fija todavía. Se revisan paso a paso cuando lleguemos a cada uno.

- [x] **Revisión y homologación de archivos de configuración — investigación por marca completa (las 4 marcas).** `dic_to_json()` (Axis/Dahua) y `txt_to_json()` (Vivotek) parsean el texto plano de configuración de cada marca con reglas distintas entre sí (separador `.` vs `_`, comillas en el valor, prefijo a omitir) porque así responde cada cámara — no es necesariamente algo a "unificar" a la fuerza. Las 4 marcas fueron probadas en vivo contra cámaras reales (ver detalle abajo). **Queda abierta, sin decidir todavía**, si conviene una función común parametrizada en vez de varias casi-iguales — no es urgente porque las 3 funciones (`dic_to_json` de Axis/Dahua y `txt_to_json` de Vivotek) ya quedaron correctas y probadas por separado.
  - **[x] AXIS — investigado y corregido.** Se probó en vivo contra 2 cámaras reales (P1365 Mk II fw 9.80.132 y P3278-LV fw 12.11.77, para cubrir la duda de si el firmware ≥11 cambia algo) usando el servicio `apidiscovery.cgi` de la propia cámara:
    - El endpoint `backup.cgi`/`restore.cgi` que se encontró en documentación antigua **no existe** en ninguna de las 2 (404 confirmado) — descartado.
    - Firmware ≥11.8 sí tiene una API adicional ("Device Configuration APIs"), pero es un framework fragmentado por dominio (red, certificados, firewall...) sin endpoint único — **no sirve para reducir a 1 sola petición**, sería al revés.
    - `param.cgi?action=list&group=root` (lo que ya usa el código) **sigue funcionando igual de bien en ambas generaciones de firmware** — confirmado en vivo (66-71 KB, ~1500-1700 líneas en cada una). No se cambió el endpoint.
    - **Bug real encontrado con datos reales (no hipotético):** `dic_to_json()` perdía 16-17 parámetros **en cada cámara, siempre** (no un caso raro) por el problema ya conocido de `split("=")` — incluyendo **las posiciones guardadas de presets de PTZ** (`root.PTZ.Preset.P0.Position.P1.Data=pan=...:tilt=...:zoom=...`). Corregido a `partition("=")` (mismo enfoque que ya usaba Dahua). Verificado en vivo contra las 2 cámaras: 0 líneas perdidas (antes 16-17 cada una), y el preset de PTZ ya se captura correctamente. Pruebas de pytest actualizadas.
  - **[x] DAHUA — investigado, rediseñado e implementado.** Probado en vivo contra una DH-IPC-HDBW3549R1-ZAS-PV (fw 3.140.0000000.81.R):
    - `configManager.cgi?action=getConfig&name=All` existe y funciona: **1 petición, 985 KB, 577 secciones reales** (vs. las 3 secciones que cubrían las 3 peticiones curadas anteriores — Network/VideoColor/Encode). No incluye cuentas de usuario/contraseñas (Dahua las excluye a propósito, 0 líneas "Account" encontradas).
    - `Config.backup?action=All` también existe (630 KB) pero en JSON ya estructurado — formato distinto, no compatible con el parser actual sin reescribirlo; se descartó a favor de `name=All`, que sí encaja con el parser existente.
    - **Implementado:** `DahuaCamConf()` pasó de **6 peticiones a 3**: `getSystemInfo` (agrupa tipo+serie real+versión de hardware, antes eran 2 llamadas separadas), `getSoftwareVersion` (firmware, no está en ningún otro lado), y `getConfig&name=All` (toda la configuración, reemplaza las 3 llamadas curadas de Network/VideoColor/Encode). `channel` ya no se usa (antes filtraba a un canal; "All" trae todos) pero se dejó en la firma por consistencia con `CAMERA_HANDLERS`.
    - **Limitación de parseo documentada en el código** (no corregida, caso extremo): si una ruta de clave aparece a la vez como valor final y como padre de otra clave más profunda (`P2PLimit.Video` y `P2PLimit.Video.resolution` a la vez), una sobreescribe a la otra — 1 caso en 15,846 líneas en la prueba real. No se puede representar sin cambiar la estructura de salida; se dejó como comentario en `dic_to_json()`.
    - Probado extremo a extremo contra la cámara real: 0.83s, 585 secciones capturadas (tipo/serie/firmware/Network/PPPoE/Email, antes ausentes), 34 pruebas de pytest pasan.
  - **[x] HIKVISION — investigado y confirmado en vivo** contra una DS-2CD2683G1-IZS (fw V5.6.821 build 230831):
    - **`GET /ISAPI/System/configurationData` confirmado como vía muerta**, tal como anticipaba la señal de alerta de la documentación: responde `400` con `statusCode: 6`, `statusString: "Invalid Content"`, `subStatusCode: "cfgFileSecretKeyError"`. Es un mecanismo de exportación/backup **cifrado** (requiere `secretkey` para desencriptar del lado del cliente), no un endpoint de lectura de configuración en texto plano — no hay forma de usarlo para este propósito sin cambiar por completo el enfoque (habría que desencriptar el archivo exportado). Descartado.
    - Los 3 endpoints de respaldo confirmados en vivo, los 3 en XML plano (200, sin cifrado): `System/time` (hora/NTP), `Security/users` (solo usuario y nivel, **sin contraseña**), `System/capabilities` (9.5 KB, esquema de qué soporta el dispositivo). Se verificó que ninguno de los 7 root tags XML (los 4 curados + estos 3) choca entre sí al fusionar con `json_conf.update(json_data)`.
    - **Implementado:** `HikvCamConf()` pasó de 4 a **7 peticiones** (se mantiene en varias peticiones, ya que `configurationData` no es viable): se agregaron `System/time`, `Security/users`, `System/capabilities` a la lista curada existente. Probado en vivo: 0.08s, 7 secciones (`DeviceInfo`, `NetworkInterface`, `ImageChannel`, `StreamingChannel`, `Time`, `UserList`, `DeviceCap`), 12.9 KB de JSON, 34 pruebas de pytest pasan.
    - Se incluyó `Security/users` a propósito (decisión del usuario): solo expone nombre de usuario y nivel de cuenta, nunca la contraseña, por lo que el riesgo es bajo.
  - **[x] VIVOTEK — investigado, rediseñado e implementado.** Probado en vivo contra una IP9181-LPC-v2 (fw 1.2203.37.01a):
    - Confirmado en vivo: `getparam.cgi` **sin ningún grupo** regresa **todos los parámetros del servidor**: 1 petición, 83,956 bytes, 2,427 líneas (vs. 16,842 bytes de los 3 grupos filtrados anteriores — `network&videoin&system`). Sigue siendo 1 sola petición.
    - **Confirmado: la contraseña de administrador NO aparece** en la respuesta completa (se revisaron todas las líneas relacionadas a `pass`/`secret`/`key`; son ajustes de configuración, no credenciales) — no hace falta filtrar nada antes de guardar el JSON.
    - **Bug real encontrado con datos reales (mismo tipo que Axis):** `txt_to_json()` usaba `split("=")` y perdía silenciosamente cualquier línea cuyo valor trajera otro `=` — 1 caso real confirmado: `vadp_number='BbM79RE=94nv7GCafhb4YxaiY0jvbz8T/9gL'` (token tipo base64). Corregido a `partition("=")`, igual que Axis/Dahua. Pruebas de pytest actualizadas (35 pasan).
    - **Hallazgo menor, no es bug:** una clave duplicada en la respuesta real (`eptz_c0_osdzoom` aparece 2 veces) con el mismo valor exacto ambas veces — rareza del firmware, no pérdida de datos real, no requiere corrección.
    - **Implementado:** `VivoCamConf()` quitó el filtro de grupos (`?network&videoin&system` → sin parámetros), sigue siendo 1 sola petición. Probado extremo a extremo contra la cámara real: 0.79s, 41 secciones de nivel superior, 2,426 hojas parseadas (2,425 + el valor con `=` ya recuperado), 51.8 KB de JSON.
- [x] **Rediseñar cómo `Cam_Config()` solicita la configuración de la cámara — investigado con datos comparativos reales, e implementado.**
  - **Comparación controlada V1 vs. V2** (mismo día, mismos 11 servidores/163 cámaras del entorno Ubuntu, `MAX_CAMERA_WORKERS=5` en ambas para aislar la variable): V1 (secuencial, sin paralelismo) tuvo 34 cámaras con al menos una falla; V2 (paralelo, Fase 2) tuvo 45. En 8 de los 11 servidores el número de fallas fue **idéntico** entre versiones (confirmando que esas fallas —servidores/plantas completos sin responder— son problemas de red/túnel reales de ese momento, no algo introducido por V2). La diferencia real se concentró casi toda en un solo servidor Hikvision (ZACATECAS/QLYMSPROD01: 4 fallas en V1 → 13 en V2, +9), con un caso más pequeño igual en QLYMSPROD02 (+1) y un patrón distinto en MEDELLIN (+1).
  - **Conclusión confirmada:** las 9-10 fallas adicionales en los servidores Hikvision son siempre el mismo patrón — `RemoteDisconnected('Remote end closed connection without response')` justo en la primera petición de `Cam_Config` (`System/deviceInfo`). Esto descarta la hipótesis original ("la cámara corta la conexión tras 6-8 peticiones acumuladas"): la falla ocurre en la petición #1, antes de acumular nada. La causa real es el **paralelismo**: con varias cámaras del mismo servidor siendo revisadas al mismo tiempo (aunque sea con solo 5 workers) a través del mismo proxy/túnel SSH, algunas conexiones hacia la cámara se cortan bajo esa concurrencia — cosa que no pasa en V1 porque procesa una cámara a la vez, sin contención.
  - **Hallazgo aparte, mismo origen (concurrencia):** en MEDELLIN, varias cámaras fallan la Imagen IA (`Timed out [601]`) exactamente en el mismo segundo — varias peticiones simultáneas al mismo servicio de IA local del servidor, que no las procesa a tiempo dentro del timeout configurado.
  - **Implementado:** `Cam_Config()` (`cameras.py`) ahora reintenta la descarga completa de configuración ante cualquier falla, usando el mismo patrón de reintentos que ya usaba `Cam_Up()` — nuevas variables `MAX_CONFIG_RETRIES` (default 2) y `CONFIG_RETRY_DELAY` (default 1s). Es seguro reintentar la llamada completa porque son solo peticiones GET (sin efectos secundarios) y porque una conexión cortada por congestión del proxy típicamente se recupera sola en el siguiente intento (confirmado en producción: en V2, las mismas cámaras que fallaban en esta corrida sí completaban bien en corridas posteriores). Aplica a las 4 marcas por igual, sin duplicar código, porque el reintento vive en el punto único donde `Cam_Config()` invoca al manejador de cada marca (`CAMERA_HANDLERS`), no dentro de cada `*CamConf()`.
  - Pruebas de pytest nuevas (`tests/test_cameras.py`, 3 casos: se recupera tras una falla transitoria, agota reintentos y devuelve el último código de error, no reintenta si la primera petición tiene éxito) — 38 pruebas pasan en total. Probado también en vivo contra la cámara Hikvision real para confirmar que el flujo normal (sin fallas) no cambia de comportamiento.
  - **Fuera de este alcance, anotado para más adelante si vuelve a aparecer:** el mismo tipo de saturación por concurrencia en el servicio de IA (`Cam_AI_Image()`) — hoy sin reintentos; no se tocó porque no fue el foco de esta comparación y no tiene la misma garantía de "reintentar arregla solo" (depende de qué tan ocupado esté el servicio de IA, no de una conexión cortada).
  - **Verificación con una 3ª corrida real (después de aplicar el fix):** `ZACATECAS/QLYMSPROD01` pasó de 13 fallas (V2 sin fix) a solo 3 — las mismas 13 cámaras volvieron a sufrir `RemoteDisconnected`, pero esta vez **las 13 se recuperaron solas en el reintento** (0 de las 13 terminó en falla final). Las 3 fallas que quedaron no tienen relación con este bug (ruido normal ya visto en V1: timeouts de puerto 80 e imagen IA).

- [x] **Bug encontrado durante la 3ª corrida de verificación — túnel SSH huérfano confundido con el túnel correcto (lectura silenciosa de datos del servidor equivocado).**
  - **Cómo se detectó:** en esa misma 3ª corrida, `QLYMSPROD02` (ZACATECAS, normalmente 92 cámaras/48 activas) reportó solo 12 cámaras — con los mismos alias e IPs exactos de `QLYMSPROD04` (MEDELLIN). `QLYMSPROD02` terminó leyendo, sin ningún error visible, los datos de otro servidor.
  - **Causa raíz:** `create_tunnel()`/`check_tunnel_status()` (`system_processor.py`) solo verificaban si *algo* estaba escuchando en el puerto local compartido del túnel (`IA_LOC_PORT`/`PX_LOC_PORT`, fijos y reutilizados secuencialmente entre servidores) — nunca confirmaban que ese "algo" fuera realmente el túnel hacia el servidor que se estaba por revisar. Al interrumpir una corrida anterior a medio camino (para hacer `git pull`), un túnel SSH de `QLYMSPROD04` quedó huérfano, todavía escuchando en ese puerto. La siguiente corrida, al crear el túnel de `QLYMSPROD02` en el mismo puerto, encontró ese túnel viejo ya "activo", lo dio por bueno sin comprobar el destino, y leyó los datos equivocados bajo la etiqueta correcta.
  - **Por qué es más grave que una falla normal:** no truena, no genera ningún código de error — simplemente atribuye datos de una planta a otra en silencio. Puede ocurrir también en producción si el programa se interrumpe a medio camino (kill, reinicio, corte de energía) y deja un túnel huérfano en el puerto compartido.
  - **Implementado (`system_processor.py`):**
    - `_tunnel_matches_destino()`: confirma que el proceso encontrado en el puerto tenga en su línea de comando exactamente el forward esperado (`-L {loc_port}:{dest_host}:{ssh_port}`) y el `remote_server` correcto, antes de confiar en él.
    - `_find_verified_tunnel_pid()`: envuelve `find_tunnel_pid()` aplicando esa verificación; los 3 puntos de `create_tunnel()` que antes confiaban ciegamente en "hay algo en el puerto" ahora usan esta versión verificada — si no coincide, se trata como falla (no se sigue de largo con datos de otro servidor).
    - `create_tunnel()` ahora también limpia proactivamente el puerto local (con `alternative_close()`) **antes** de intentar crear el túnel nuevo, para no depender de que un túnel huérfano de una corrida anterior se identifique correctamente — simplemente no debe seguir ahí.
  - Pruebas de pytest nuevas (`tests/test_system_processor.py`, 7 casos, incluyendo el caso real: un túnel con el mismo puerto local pero apuntando a otro `remote_server` debe rechazarse) — 45 pruebas pasan en total. Probado también en vivo (puerto libre, y creación de túnel contra un host SSH inexistente) para confirmar que no rompe el flujo normal ni las rutas de error existentes.
  - **Pendiente del lado de infraestructura (no es código):** el usuario revisó el Ubuntu Server para detener manualmente los procesos `ssh` huérfanos que quedaron de la corrida interrumpida.
  - **Verificación con una 4ª corrida real, después de limpiar los túneles huérfanos:** `QLYMSPROD02` volvió a mostrar correctamente sus propias 92 cámaras/48 activas (alias e IPs reales de ZACATECAS-B), ya no los datos de MEDELLIN — 5 fallas, en línea con lo ya visto en V1 (4) y V2 (5), confirmando que la corrección es correcta y no introduce fallas nuevas. `QLYMSPROD01` repitió el mismo resultado que la corrida anterior: 13 `RemoteDisconnected` recuperados solos por el reintento, 0 fallas finales por esa causa — segunda corrida consecutiva confirmando el fix de `Cam_Config()`.
  - **Nota metodológica para futuras comparaciones:** los `.log` por servidor usan `FileHandler` en modo *append* — si se corre el programa más de una vez el mismo día sin limpiar la carpeta de resultados en el servidor, el archivo de un servidor acumula el contenido de corridas anteriores. El `resumen_dd-mm-aaaa.log` sí se genera limpio en cada corrida (refleja solo la última), así que es la fuente confiable para comparar corridas del mismo día; si se necesita el detalle por cámara de una corrida específica, hay que tomar solo la parte del `.log` posterior al último "Total de cámaras".

- [x] **Mejora de log: cada línea de cámara ahora muestra el proceso, no solo la cámara.** El usuario notó que una línea de error como `A4t1: Timed out [111]` dice qué cámara falló pero no en qué paso (Puerto 80, Imagen IA, Imagen cámara o Configuración) — varios códigos se comparten entre procesos (ej. el timeout de Imagen IA y el de Imagen cámara usan el mismo `611`), así que antes no se podía distinguir solo con el código.
  - **Formato acordado:** `[hora] NIVEL   alias: proceso          descripción          [código]`. En éxito, la descripción se simplifica a `OK` (antes el texto mismo del código ya decía el proceso, ej. "Port 80"/"IA Image Save", quedaría redundante con el nuevo campo).
  - **Implementado:** `log_processor()` (`file_processor.py`) acepta un parámetro opcional `proceso`; nueva función `_status_parts()` separa la descripción del código entre corchetes. Los otros ~40 usos existentes de `log_processor()` (ping, túneles, YAML, etc.) no pasan `proceso` y siguen exactamente igual. `check.py` pasa `proceso="Puerto 80"/"Imagen IA"/"Imagen cámara"/"Configuración"` en las 4 llamadas por cámara en `_process_camera()`.
  - Pruebas de pytest nuevas para `_status_parts()` — 48 pruebas pasan en total. Probado en vivo generando un log de muestra con los 4 procesos y casos de éxito/error para confirmar el formato final.
  - **Bug real encontrado al armar la muestra, corregido de paso:** `AxisCamConf()` y `VivoCamConf()` devolvían `701` en timeout de configuración, pero ese código **nunca estuvo registrado** en `config.STATUS_MESSAGES` (solo existe `711` con ese mismo texto) — como consecuencia, esas fallas se logueaban en **verde (INFO)** en vez de rojo (ERROR), porque el código desconocido no tiene el prefijo `[ERROR]` que decide el color. Ya se había visto en producción (la cámara `CA` en varias corridas de hoy). Corregido a `return 711` en ambos archivos (Dahua/Hikvision no tenían este bug: ya usaban `790` genérico para timeout de configuración).

- [x] **Bug de observabilidad: un servidor caído o uno perfecto se veían igual de ausentes en el resumen.** Detectado por el usuario al comparar la corrida del 18 (`TOCANCIPA` inalcanzable) con otras normales: `resumen_dd-mm-aaaa.log` solo lista **excepciones** — `TOCANCIPA` solo aparecía en la lista corta "Servidores con problemas" del encabezado (fácil de pasar por alto si se revisa directo la sección de fallas por planta), y un servidor perfecto (`TUXTEPEC` ese día) **no aparecía en ningún lado**, exactamente igual que si nunca se hubiera revisado. No había forma de distinguir "todo bien" de "nunca se revisó" con solo mirar el resumen.
  - **Implementado (`write_summary()`, `file_processor.py`):** nueva sección `DETALLE POR SERVIDOR`, una línea por cada servidor de la corrida, siempre presente, con estado explícito: `[OK]` (todas sus cámaras completas), `[CON FALLAS]` (X/Y completas), `[SIN CONEXIÓN]` (el servidor mismo nunca respondió, 0 cámaras revisadas). Reemplaza la lista "Servidores con problemas" del encabezado (quedaba redundante). El resto del resumen (totales generales, `DETALLE POR PLANTA - CÁMARAS CON FALLAS`) sigue igual.
  - Validado con los datos reales de la corrida del 18 (`Desktop\Test\180926`): se mostró el mockup exacto al usuario antes de implementar, y el resumen generado con esos mismos datos coincidió carácter por carácter con lo aprobado.
  - Pruebas de pytest nuevas (`tests/test_file_processor.py`, `TestWriteSummaryDetallePorServidor`, 4 casos: servidor OK, servidor con fallas, servidor sin conexión que ya no desaparece, y que todos los servidores aparecen aunque estén perfectos) — 52 pruebas pasan en total.

- [x] **Reordenar los logs (por servidor y el resumen) — pedido explícito del usuario, con una decisión de arquitectura importante.**
  - **El pedido:** el log por servidor debe quedar organizado en 4 bloques fijos (Encabezado → Túneles → YAML → Cámaras), con cada cámara en un bloque contiguo (hoy se intercalan entre sí por correr en paralelo) y columnas alineadas en todo el documento. El resumen (`DETALLE POR SERVIDOR` y `DETALLE POR PLANTA`) debe ordenarse por una estrategia configurable por cliente/entorno (AbInBev: por el número al final del nombre del servidor; Others/Windows: alfabético) en vez del orden de aparición.
  - **Decisión de arquitectura (Opción A, elegida explícitamente por el usuario sobre la alternativa de solo bufferear en memoria):** el log se sigue escribiendo en vivo, línea por línea, exactamente igual que antes (robustez ante un corte a medio camino) — pero justo después de terminar de revisar las cámaras de un servidor, la sección de cámaras se **recorta y se reemplaza** por una versión limpia, ordenada y tabulada, construida desde los datos estructurados que ya se recopilan (no se relee ni se parsea el texto del log). El encabezado/túneles/YAML nunca se tocan, porque esos ya se escriben en orden (no sufren el problema del paralelismo).
  - **Implementado (`file_processor.py`):**
    - `checkpoint_server_log(plant, server)`: guarda la posición (bytes) del log justo antes de empezar a revisar cámaras.
    - `replace_camera_section(plant, server, checkpoint, camera_results)`: recorta el archivo de vuelta a ese punto (`seek` + `truncate` sobre el `FileHandler` ya abierto) y escribe el bloque de cámaras ya ordenado alfabéticamente por alias, con el alias tabulado al ancho del más largo de ese servidor. Probado a mano que lo que se loguea después del reemplazo (ej. cierre de túnel) se sigue agregando bien al final.
    - `write_summary()`: `DETALLE POR SERVIDOR` y `DETALLE POR PLANTA` ahora se ordenan con `sort_strategies.get_sort_key(config.LOG_SORT_STRATEGY)`. `DETALLE POR PLANTA` pasó de agrupar por planta a agrupar por **servidor** (con separador `====` entre servidores de una misma planta), y las cámaras fallidas dentro de cada servidor se ordenan por tipo de falla (Puerto 80 → Imagen IA → Imagen cámara → Configuración, `FAILURE_TYPE_PRIORITY`) y luego alfabéticamente por alias. Se descartó agregar "Tiempo de proceso" como un tipo de falla más (el usuario lo pidió al inicio y luego confirmó que no hacía falta).
  - **Nuevo módulo `sort_strategies.py` (aditivo, no toca código existente) — decisión de diseño discutida con el usuario:** se consideró un sistema de reglas en YAML/JSON por cliente (carpeta `/log_sort`), pero se descartó a favor de un **registro con nombre en Python** (`SORT_STRATEGIES = {"numeric_suffix": ..., "alphabetical": ...}`, seleccionado por `LOG_SORT_STRATEGY` en `.env`) — mismo resultado práctico (un cliente nuevo con un patrón ya soportado solo necesita una línea en su `.env`), pero sin construir un mini-lenguaje de reglas declarativas para necesidades hipotéticas que todavía no existen (ej. un cliente futuro tipo "BEPENSA"). Si aparece un patrón genuinamente nuevo, se agrega una función chica y probada al registro.
  - `_process_camera()` (`check.py`) ahora también captura, sin quitar nada del logueo en vivo existente: `steps` (hora + proceso + código de cada paso que corrió), `hora_ip`, `elapsed` y `hora_tiempo` — es lo que `replace_camera_section()` necesita para reconstruir el bloque completo de cada cámara.
  - Probado extremo a extremo con cámaras falsas corriendo en paralelo (`ThreadPoolExecutor`, igual que en producción): el archivo final quedó con las cámaras en orden alfabético, cada una en su bloque completo, columnas alineadas, y el resto del log (encabezado, YAML, lo que se loguea después del reemplazo) intacto.
  - Pruebas de pytest nuevas: `tests/test_sort_strategies.py` (5 casos) y `TestWriteSummaryDetallePorPlanta` en `tests/test_file_processor.py` (4 casos: orden por tipo de falla, cámara con varias fallas usa la más temprana, separador entre servidores de una misma planta, plantas ordenadas por la estrategia configurada) — 62 pruebas pasan en total.
  - **Bug real encontrado al probar en ambos entornos:** `DETALLE POR SERVIDOR` usaba anchos de columna **fijos** (`{plant:<13}`) — con un nombre de planta más largo de lo esperado (`API-MANZANILLO`, 14 caracteres, entorno Others/Windows) el relleno no alcanzaba y el nombre de la planta quedaba pegado directo con el del servidor, sin espacio (`API-MANZANILLOAPIMAN-FASE1`). Corregido a anchos **dinámicos**, calculados sobre el contenido real de esa corrida (mismo enfoque que ya usaba `DETALLE POR PLANTA` para sus columnas). Probado en vivo contra los dos entornos reales (AbInBev: orden 01→11 correcto con separadores; Others: ya no se pega ninguna columna) — 63 pruebas pasan en total.

- [x] **Bug real en cascada: un túnel huérfano por falla de YAML tumbaba la revisión de TODOS los servidores siguientes.** Detectado por el usuario en una corrida real: `QBYMSPROD08` falló al leer su YAML (`Connection error [410]`), y a partir de ahí `VALLE`, `GUADALAJARA` y `ATLANTICO` fallaron exactamente igual, en cascada — y el resumen los marcaba a todos como `[OK] 0/0 cámaras completas`, ocultando el problema por completo. Eran en realidad **3 bugs encadenados**:
  1. **`CheckServer()` (`check.py`) solo cerraba el túnel PX, nunca el IA, cuando el YAML fallaba.** El cierre del túnel IA estaba anidado dentro de `if status_code == 400:` — si el YAML fallaba, ese bloque se saltaba entero, incluyendo el cierre. El túnel IA quedaba huérfano, ocupando el puerto local compartido (`IA_LOC_PORT`) para el resto de la corrida. **Corregido:** el cierre del túnel IA se movió fuera de ese bloque, al mismo nivel que el del túnel PX — se cierra siempre que se haya abierto, sin importar si el YAML falló.
  2. **`alternative_close()` (`system_processor.py`) nunca lograba matar el túnel huérfano, aunque lo detectaba correctamente.** El mecanismo de "limpiar el puerto antes de crear" (agregado hace unos días) sí detectaba el conflicto (`"posible túnel huérfano. Cerrándolo primero"`), pero el patrón de búsqueda (`f":{loc_port}"`) nunca coincidía con la línea de comando real de un túnel SSH — el puerto **local** de un forward (`-L 9045:localhost:8045`) va precedido de un espacio, no de `:`. **Corregido** a `f"-L {loc_port}:"`, el patrón correcto.
  3. **`write_summary()` marcaba como `[OK]` un servidor que nunca llegó a revisar ninguna cámara**, porque `completas == total` es `0 == 0` → `True`. **Corregido:** `CheckServer()` ahora registra un `problem` explícito (con `problem_type = "sin_yaml"`) cuando ningún puerto IA logra leer su YAML; el resumen usa una etiqueta nueva y explícita, **`[SIN YAML]`**, distinta de `[SIN CONEXIÓN]` (que sigue significando "el servidor mismo nunca respondió") — decisión tomada junto con el usuario, que pidió algo "más descriptivo" que reusar la etiqueta existente.
  - **Nota aparte, no es un bug de hoy:** si un servidor tuviera más de un `ia_port`, el túnel PX se cierra después del primer puerto y quedaría cerrado para el segundo — comportamiento preexistente (no introducido por este fix), y no aplica a ningún servidor real visto hasta ahora (todos usan un solo `ia_port`). Queda anotado por si algún día importa.
  - Pruebas de pytest nuevas: `TestAlternativeClose` en `tests/test_system_processor.py` (3 casos, incluyendo el caso real: un túnel con el puerto buscado como destino pero no como local no debe matarse), `test_falla_yaml_en_servidor_con_tuneles_cierra_ambos_y_marca_problema` en `tests/test_check.py` (reproduce el escenario completo: YAML falla en un servidor `type: tunnels`, confirma que se cierran AMBOS túneles y que `problem_type == "sin_yaml"`), y `test_servidor_sin_yaml_usa_su_propia_etiqueta` en `tests/test_file_processor.py` — 68 pruebas pasan en total.

- [x] **3 ajustes pedidos por el usuario tras revisar la corrida real donde se confirmaron los fixes de arriba.**
  1. **`[SIN YAML]` mostraba el mensaje de error completo en el resumen**, redundante con el detalle que ya está en el log de esa planta, y rompía la alineación visual de la columna (única fila que no seguía el patrón `X/Y cámaras completas`). Cambiado a `-/- cámaras completas` — el guion en vez de un número dice explícitamente "no se sabe cuántas cámaras hay" (nunca se leyó el YAML), a diferencia de un `0/0` que insinuaría que sí se determinó un total. Decisión tomada junto con el usuario (propuso `-/-` para mantener el mismo patrón visual que las demás filas).
  2. **Faltaban separadores `====` al cerrar túneles.** Al abrir, cada túnel (PX, IA) tiene su propio separador inmediatamente después — al cerrar, antes solo se emitía un separador al final de ambos cierres juntos, no uno por cada uno. Además, cuando el YAML fallaba, no había ningún separador entre la línea de error y el inicio del cierre de túneles, dejándolos visualmente pegados. **Corregido:** separador después de cada `close_tunnel()` (IA y PX, cada uno el suyo, simétrico con la apertura) y separador también al final del bloque YAML cuando falla (antes solo existía en el camino exitoso, como parte del bloque "Total de cámaras...").
  3. **Aumento real de `Imagen cámara: Timed out`** (3 → 1 → 10 en 3 días consecutivos, confirmado con los resúmenes de cada corrida). Se revisó el log de `TOCANCIPA` de la corrida con más fallas y se encontró el mismo patrón que ya se diagnosticó para `Cam_Config()`: varias cámaras fallando casi al mismo segundo exacto, señal de congestión transitoria del proxy/túnel bajo concurrencia, no cámaras individualmente caídas. **Causa de fondo:** a diferencia de `Cam_Config()` (que ya tiene reintentos desde hace unos días), `Cam_Image()` no tenía ningún reintento, y `Cam_AI_Image()` sí reintentaba pero **sin ninguna espera entre intentos** (reintentaba de inmediato, sin darle tiempo a la congestión a despejarse). **Implementado (`cameras.py`):** `Cam_Image()` ahora reintenta la llamada completa al manejador de la marca, mismo patrón que `Cam_Config()` (reutiliza `MAX_IMAGE_RETRIES`, ya existente); `Cam_AI_Image()` ahora espera `IMAGE_RETRY_DELAY` segundos entre intentos en vez de reintentar en el acto. Nueva variable `IMAGE_RETRY_DELAY` (default 1s) en `config.py`/`.env.example`.
  - Pruebas de pytest nuevas: `TestCamImageReintentos` (3 casos) y `TestCamAIImageReintentos` (confirma la espera entre intentos) en `tests/test_cameras.py`, `test_servidor_sin_yaml_usa_su_propia_etiqueta` actualizado al formato `-/-` — 72 pruebas pasan en total. Probado en vivo simulando `CheckServer()` completo con una falla de YAML en un servidor `type: tunnels`: confirmado que el archivo final queda con un separador por cada bloque (YAML fallido, cierre IA, cierre PX), igual que en la apertura.

- [x] **`[SIN CONEXIÓN]` tenía el mismo problema que `[SIN YAML]` — mismo fix, aplicado por consistencia.** El usuario notó, al revisar la corrida siguiente, que `[SIN CONEXIÓN]` seguía mostrando el mensaje completo (`Sin IP disponible (zerotier/local/cámaras)`) en vez de `-/-` — se había corregido `[SIN YAML]` pero no se generalizó al otro estado con el mismo problema. Unificado: ambos usan ahora `-/- cámaras completas`, mismo criterio (el estado ya dice el motivo general, el detalle vive en el log de esa planta).
  - **Nota metodológica:** al revisar la corrida que el usuario pidió analizar para el punto de `Cam_Image()`/`Cam_AI_Image()`, se encontró que la carpeta más reciente (`210926`) era una copia byte-por-byte de la corrida anterior (`210926_1`, incluyendo timestamps) — no una corrida nueva. Se le pidió confirmar el `git pull` en el Ubuntu Server antes de repetir la prueba; pendiente de datos frescos para evaluar si el fix de reintentos de imagen tuvo efecto real.

---

## Último paso — Documentación

Se hace al final de todo, una vez que el código ya no se va a seguir moviendo — así se documenta una sola vez el estado final, en vez de tener que reescribirlo cada vez que algo cambie mientras avanzamos.

- [x] **`README.md` del proyecto.** Incluye: requisitos previos, instalación, los dos entornos de ejecución, explicación completa de `.env` y `plants.yaml` (con tablas de cada campo), plantillas `.env.example` y `conf/plants/plants.yaml.example` (nombradas así — no `example.env`/`plants.example.yaml` — para no caer en los patrones del `.gitignore`, verificado con `git check-ignore`), uso del programa, procedimiento paso a paso, marcas soportadas y cómo agregar una nueva, qué resultados entrega, cómo leer el log, y cómo correr las pruebas. Plantillas validadas: el YAML parsea correcto con todos los campos requeridos, y las variables del `.env.example` coinciden exactamente con lo que lee `config.py` (sin faltantes ni sobras).

---

## Fuera de alcance para V2 (posible V3)

- Dashboard web / interfaz gráfica.
- Base de datos histórica de resultados.
- Alertas automáticas (correo/Slack/Teams) ante fallas.
